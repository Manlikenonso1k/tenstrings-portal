CONTEXT: My Laravel 11 / Filament v3 CSV import is running in the background, but the UI is silent. I have no progress bar, and I don't receive a summary of skipped or duplicated rows.

TASK: ENABLE REAL-TIME IMPORT FEEDBACK

Database Notifications: Ensure Filament's Database Notifications are enabled so the import summary can be "popped up" in the admin panel once finished.

Broadcasting & Queues: Configure the ImportAction to ensure it uses the database driver for notifications.

The Progress UI: Update the StudentImporter to explicitly handle the getJobProgress() or getImportSummary() so the user sees a real-time percentage bar in the top-right notification center.

Detailed Reports: Ensure that once the import is "Done," a notification is sent to the Super Admin containing:

A link to download the failed/skipped rows CSV.

The count of successful vs. duplicated entries.

TECHNICAL REQUIREMENTS:

Provide the command to create the notifications table if missing.

Provide the code for the ImportAction that triggers the progress bar.

Explain how to verify that the queue:work command is correctly updating the Filament imports table.

OUTPUT: A step-by-step fix for the silent import issue and the code to display the final report modal.