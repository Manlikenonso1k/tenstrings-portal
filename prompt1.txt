THE "CLEAN-PATH LARAVEL REDIRECT" PROMPT
CONTEXT: I am finalizing the routing for the Tenstrings Portal (Laravel 11). The application is hosted on the portal.tenstrings.org subdomain, which shares a root directory with a WordPress installation.

TASK: Implement a clean, code-based redirect in the Laravel application to handle root-level traffic.

TECHNICAL REQUIREMENTS:

File Location: Update the routes/web.php file at the very top of the route definitions.

Redirect Logic: When a user visits the base URL (/), they must be immediately redirected to the Filament login page at /portal/login.

Authentication Awareness: >     - If a user is already logged in, they should be redirected to the main portal dashboard (/portal) instead of the login screen.

If a user is a guest, they should be sent to /portal/login.

Performance: Use Laravel's built-in redirect() helper or Route::redirect() for the fastest execution.

Clean-up: Ensure no other "welcome" or default Laravel landing page routes are active that might interfere with this redirect.

OUTPUT: Provide the updated routes/web.php code snippet with the necessary imports and logic.