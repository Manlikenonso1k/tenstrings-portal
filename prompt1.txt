CONTEXT: I am building the "Tenstrings Portal". We need to refine the registration flow and create a high-end, organized Student Data dashboard in Filament.

TASK 1: SMART MATRIC GENERATION & COURSE SELECTION

Update the register/student form to include a Course Selection dropdown using the 15 specific courses provided (e.g., "Advance Diploma in Music Performance - 18 months").

Matric Logic: Rewrite the MatricNumberGenerator to follow this pattern: [YEAR][MONTH][COURSE_CODE][SEQUENCE].

Example: 202603ADMP001.

Ensure each of the 15 courses has a unique shortcode (ADMP, ADPD, DMP, etc.) mapped for the matric number.

TASK 2: STUDENT DATA UI REFACTOR (GRID LAYOUT)
The current student data view is cluttered. Refactor the StudentResource (and the Student Portal View) into a clean, 4-column grid layout with the following specific sections:

COLUMN 1: IDENTITY & PASSPORT

Update Passport: A dedicated FileUpload component that updates the avatar_url for the dashboard.

COLUMN 2: CORE INFORMATION

Basic Data: A Section containing: Full Name, Address, Email, Phone Number, and Guardian Phone Number.

COLUMN 3: ACADEMIC & PROGRESS

Education History: A Section displaying "Quarterly Assessment Stats" (Feb, May, Aug, Nov).

Print Center: Two Action buttons:

Print Admission Letter (Generates a PDF).

Print Bio-data (Generates a PDF summary of the user's profile).

COLUMN 4: DOCUMENT VAULT

Document Upload: A FileUpload for Birth Certificate, JAMB, NECO, and WAEC.

Constraints: Allowed: pdf, png, jpg, jpeg. Max size: 2mb.

TASK 3: PDF GENERATION LOGIC
Provide a simple Laravel controller or Filament action logic to handle the PDF generation for the Admission Letter and Bio-data using barryvdh/laravel-dompdf.

OUTPUT: Provide the updated Register.php form schema, the MatricNumberGenerator logic, and the refactored StudentResource schema.