TASK: Refactor the custom Filament login page to resolve layout conflicts, fix dark mode visibility issues, and apply strict dimension constraints.

1. LAYOUT & RESPONSIVENESS:

Implement a two-column layout for desktop: Logo Div on the LEFT, Form Div on the RIGHT.

MOBILE VIEW: Hide the Logo Div entirely; the Form Div should take up the full width.

DIMENSIONS: The parent container must have a fixed height of 489.594px and a width of 1024px (centered on desktop).

2. TEMPLATE CLEANUP:

Remove all non-essential render hooks and vendor boilerplate.

Reduce the Blade template to a minimal single-root variant to prevent "div soup" and styling inheritance bugs.

3. DARK MODE & COLOR RESOLUTION:

The Issue: There is a conflict between the custom page styling and Filament's vendor dark mode, causing white text to appear on white backgrounds.

The Fix: Explicitly set the Form Div background to bg-white dark:bg-gray-900 and text to text-gray-900 dark:text-white.

Ensure the parent background is consistent and does not "leak" into the form fields.

4. FINAL CHECKLIST (TO-DO):
Please provide a status report once finished confirming the following:

[ ] Logo is on the Left, Form is on the Right (Desktop).

[ ] Logo is hidden on Mobile.

[ ] Container Height is exactly 489.594px.

[ ] Container Width is exactly 1024px.

[ ] Template is reduced to a single-root variant.

[ ] Dark mode contrast verified (No white-on-white text in form fields).

[ ] Non-essential render hooks removed.