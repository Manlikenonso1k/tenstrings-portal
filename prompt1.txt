CONTEXT: I am expanding the analytics dashboard for the Tenstrings Portal (Laravel 11 + Filament v3). I need to track historical data across multiple years and compare performance across different branches.

TASK: ADVANCED FILTERING & BRANCH COMPARISON

Dual-Filter System: Update the FinanceChart and EnrollmentChart widgets to include two select filters:

Year Filter: A dropdown allowing the user to select the year (e.g., 2024, 2025, 2026). Default should be the current year.

Quarterly Filter: (Existing) Feb/May/Aug/Nov logic.

Branch-Specific Charts: Create three new charts (Bar or Radar charts are recommended):

Branch Enrollment: Total students per branch.

Branch Revenue: Total fees_paid per branch.

Branch Pending: Total balance_due per branch.

Handling Legacy Data: For student records where the branch column is null or empty, categorize them under a custom label called "Legacy/Unassigned" in the charts. Use a distinct color (like Grey) for this category so it stands out as data that needs cleaning.

Data Logic: >     * The charts must dynamically update when either the Year or Quarter filter is changed.

Use Eloquent queries with whereYear() and groupBy('branch_name').

Layout: Position the Branch Revenue vs. Pending bar chart as the main feature at the top, and a Student Enrollment by Branch doughnut chart on the side.

OUTPUT: Provide the updated PHP code for the widgets, the filtering logic for the Year/Quarter combination, and the SQL/Eloquent logic to handle null branch values.