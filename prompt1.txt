CONTEXT: I am adding data visualization to the Tenstrings Portal (Laravel 11 + Filament v3). I need a visual dashboard at the top of the Admin home page to track money.

TASK: CREATE A FILTERABLE FINANCIAL CHART WIDGET

Widget Type: Create a Filament ChartWidget.

Data Sources: >    - Revenue: Sum of fees_paid from the Student model.

Pending: Sum of balance_due from the Student model.

Chart Options: Implement a Bar Chart to compare Revenue vs. Pending, and a Doughnut Chart to show the ratio of paid vs. unpaid fees.
use color green ,blue and red to represent data and when filtered an animation to show the chart growth when refreshed 

Quarterly Filtering: Add a custom filter dropdown on the widget. The options must be:

Q1: February – April

Q2: May – July

Q3: August – October

Q4: November – January

Visual Styling: >    - Use Tenstrings Blue for Revenue.

Use a Warning Amber/Red for Pending fees.

Ensure the widget is set to full-width or grid-column-span-2 to sit at the top of the dashboard.

Performance: Ensure the queries are optimized and use getFilters() to refresh the chart data dynamically when a new period is selected.

TECHNICAL REQUIREMENTS:

Use Chart.js (built into Filament).

Provide the code for the FinanceChart widget class.

Provide the getHeaderWidgets() configuration for the Admin/Dashboard page.

OUTPUT: Complete PHP code for the widget and registration instructions.