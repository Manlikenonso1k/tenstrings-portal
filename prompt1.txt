CONTEXT: I have successfully set up the back-end for queued CSV imports in Laravel 11 / Filament v3. The imports table is correctly tracking processed_rows and total_rows, but I want a visual progress bar on the Admin Dashboard that updates in real-time.TASK: CREATE A POLLING PROGRESS WIDGETWidget Type: Create a custom Filament StatsOverviewWidget or a basic Widget called ActiveImportTracker.Polling Logic: Set the widget to poll every 5 seconds (protected static ?string $pollingInterval = '5s';).Data Query: >    * Find the most recent record in the imports table where completed_at is null.Calculate the percentage: $(processed\_rows / total\_rows) * 100$.UI Components:Display a Progress Bar (using a simple Tailwind CSS div or an HTML5 <progress> tag).Show the text: "Importing Students: 450 / 1000 rows processed (45%)".Include a "Live" pulsing green icon to show the process is active.Conditional Visibility: The widget should disappear (return an empty view) if there are no active imports, so it doesn't clutter the dashboard when no one is importing data.OUTPUT: Provide the PHP class for the widget and the Blade template for the progress bar.