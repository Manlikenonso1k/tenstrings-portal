CONTEXT: I am migrating the "Tenstrings Portal" from an SQLite database to a MySQL database on a production server (Hostinger). I have two applications: a Laravel 11 Dashboard and a WordPress main site.

TASK 1: DATABASE ARCHITECTURE

Prepare the MySQL database structure to hold all tables for both Laravel and WordPress.

For Laravel: Use the existing migration files to recreate all tables (users, students, courses, matric_settings, etc.) in the new MySQL database.

For WordPress: Ensure the table prefix (e.g., wp_) is respected so it doesn't conflict with Laravel tables if they share a database.

TASK 2: DATA MIGRATION LOGIC

Provide a strategy or script to export data from the current database.sqlite file and import it into the new MySQL tables without losing student records or matriculation numbers.

Fix any syntax differences between SQLite and MySQL (e.g., boolean types, auto-increments, and foreign key constraints).

TASK 3: CONNECTION CONFIGURATIONS

Provide the exact .env configuration for Laravel.

Provide the exact wp-config.php database constants for WordPress.

Ensure both applications are pointing to the same DB Host (usually localhost or a specific IP provided by Hostinger).

TASK 4: OPTIMIZATION

Set up proper indexing on the student_number (matric) and email columns in MySQL for faster login.

Verify that the storage:link and file paths for student passports are still valid after the database move.

OUTPUT: Provide the .env settings, the wp-config.php settings, and the commands to run the migrations.