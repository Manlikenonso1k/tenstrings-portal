CONTEXT: I am resolving import failures in the Tenstrings Portal (Laravel 11 + Filament v3). Several rows (7, 27, 42, 43, 50, 53) are failing due to missing/invalid emails or duplicate conflicts.

TASK 1: GRACEFUL VALIDATION FIXES

Email Repair: In the StudentImporter class, implement a beforeValidate or mutateFormDataBeforeCreate hook to:

Row 42/43 Fix: Replace commas with dots in email strings (e.g., gmail,com -> gmail.com).

Row 27/50/53 Fallback: If the email field is empty, generate a temporary placeholder email using the student's name and matric number (e.g., student_name_123@tenstrings.org).

Duplicate Strategy (Row 7): If dzeegaunded@gmail.com exists, do not fail the import. Instead, use an Upsert to update the existing record with the new CSV data.

TASK 2: BRANCH NOTIFICATION LOGIC

Trigger: Whenever a student is created or updated via the importer, trigger an automated email containing their Matriculation Number and Password.

Routing Logic:

If branch is Ikeja → Send to tenstringsikeja@gmail.com.

If branch is Agege → Send to tenstringsagege@gmail.com.

If branch is Ajah (or if the branch field is NULL/Empty) → Send to tenstringsajah@gmail.com.

send all the data to victorynonso9@gmail.com as master email oof all branches

Data Priority: Ensure the email subject is clear: "New Student Credentials: [Student Name]".

TASK 3: SUMMARY REPORT

After the import, generate a summary for the Super Admin listing exactly which "placeholder" emails were created so they can be updated with real emails later.

OUTPUT: Provide the updated StudentImporter logic, the Mail class for branch notifications, and the regex for email cleaning.