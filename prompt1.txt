I am building a Laravel 11 application called "Tenstrings Portal" using Filament. I currently have a StudentResource and an Admin panel.

TASK: I need to separate the Student and Admin login experiences into two distinct panels.

REQUIREMENTS:

Admin Panel: Keep at /admin. This is for Super Admins and staff.

Student Panel: Create a new panel at /portal. This is exclusively for students.

Authentication:

Students should have their own dedicated login page at /portal/login.

Admins should stay at /admin/login.

Logic:

Show me how to configure the StudentPanelProvider.php to include only specific resources (like StudentResource or a ResultsResource).

Provide the canAccessPanel logic for the User.php model to ensure a Student cannot access the Admin panel and vice versa.

Refactoring: Since I already have app/Filament/Resources/StudentResource.php, explain how to move or register it so it only appears in the Student Portal, not the Admin backend.

OUTPUT: Give me the raw PHP code for the StudentPanelProvider and the updated User model. Keep it concise and professional.