CRITICAL TASK: The Enrollment page at https://portal.tenstrings.org/admin/enrollments is currently empty even after students register via the custom form. Fix the data pipeline so registrations appear immediately.

1. AUDIT DATA SOURCE:

Check if EnrollmentResource is a separate model/table or just a filtered view of the Student model.

If it’s a separate table: Update the registration logic at /register/student to ensure that every time a student is created, an entry is ALSO created in the enrollments table, using the same start_date.

If it’s a filtered view: Ensure the EnrollmentResource query is specifically looking at the start_date column and not created_at.

2. MODEL CASTING:

Go to the Student model. Ensure start_date is in the $fills array and is cast as a date: protected $casts = ['start_date' => 'date'];. Without this, Filament's filters and charts cannot "read" the date.

3. RESOURCE QUERY REFACTOR:

Open app/Filament/Resources/EnrollmentResource.php (or the equivalent).

Update the getEloquentQuery() method to ensure it includes all students where start_date is NOT NULL.

Ensure the Table Columns use Tables\Columns\TextColumn::make('start_date')->date()->sortable().

4. GLOBAL SCOPE CHECK:

Check if there are any "Global Scopes" or "Tenancy" restrictions blocking the Admin from seeing these new records.

5. REFRESH WIDGETS:

Once the table data shows up, ensure the Enrollment Widget is also updated to use the start_date count.

CONFIRMATION: Show me the specific code change that allows the registration data to finally appear on the Admin Enrollment page.