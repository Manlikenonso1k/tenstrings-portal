The Direct Integration UI Redirect allows merchants to integrate TGIPAY's payment interface directly into their websites. When customers click to make payment from the merchant's application, they are redirected to TGIPAY's secure payment page where they can complete the transaction using all available payment channels.

Prerequisites
Before implementing the Direct Integration UI Redirect, merchants must complete the following setup steps:

1. Merchant Portal Registration and Verification
Register and complete verification at TGIPAY Merchant Portal
Ensure all required business documents are uploaded and approved
Complete KYC (Know Your Customer) verification process
2. Configure Payment Channels
Navigate to Settings > Preferences in the merchant portal:

Access: https://merchant.tgipay.com/settings
Select and configure the payment channels you want to offer to customers
Available channels include: Card payments, USSD, Bank transfers, and other local payment methods
3. Generate API Credentials
Go to Integrations menu in the merchant portal:

Generate Public Key (used by your backend when calling TGIPAY integration endpoints)
Generate Private Key (for server-side operations)
Configure Test Callback URL - this URL should accept callback parameters and handle success or failed status responses
interface CallbackParams {
  status: 'success' | 'failed';
  transactionReference: string;
  // Additional callback parameters
}

Implementation Guide
Step 1: Initiate Payment Transaction
Make a POST request to initiate the payment transaction from your backend:

Do not initiate payments from the frontend
Do not call POST /integration/api/v1/payment/initiate directly from browser or mobile client code.

For payment integrations, the global security standard is to create payment sessions server-to-server so the merchant backend remains the trusted source of truth for amount validation, transaction-reference generation, order integrity checks, fraud screening, logging, and audit controls. Calling this endpoint from the frontend weakens those controls and increases the risk of manipulated requests and unauthorized transaction creation.

Endpoint:

POST https://integration-service.tgipay.com/integration/api/v1/payment/initiate

Headers:

{
  "Content-Type": "application/json",
  "integration-key": "YOUR_PUBLIC_KEY"
}

Request Payload:

interface PaymentInitiationRequest {
  customerFirstName: string;
  customerLastName: string;
  customerEmail: string;
  amount: number;           // Amount in NAIRA 
  transactionReference: string;  // Unique UUID generated by merchant
  currency: "NGN";
}

Example Request:

{
  "customerFirstName": "Frank",
  "customerLastName": "Johnson",
  "customerEmail": "frank.johnson+1@mailcrop.cc",
  "amount": 1000,
  "transactionReference": "557dd54d-d27f-4ae2-96f0-eeb72f0c15c1",
  "currency": "NGN"
}

Your frontend should call your own backend endpoint, and your backend should then call TGIPAY.

Response:

{
  "customerFirstName": "Frank",
  "customerLastName": "Johnson",
  "customerEmail": "frank.johnson+1@mailcrop.cc",
  "amount": 1000,
  "transactionReference": "557dd54d-d27f-4ae2-96f0-eeb72f0c15c1",
  "currency": "NGN"
}

Step 2: Get Payment URL
Use the transactionReference from the previous response to get the payment URL:

Endpoint:

GET https://integration-service.tgipay.com/integration/api/v1/payment/{transactionReference}

Example Request:

GET https://integration-service.tgipay.com/integration/api/v1/payment/557dd54d-d27f-4ae2-96f0-eeb72f0c15c1


Response:

{
  "status": true,
  "message": "Success",
  "data": {
    "id": 17,
    "businessId": 13,
    "businessName": "Your Business Name",
    "businessLogoUrl": null,
    "accessCode": "1ebf7d26-1581-41b1-8811-4f882a85b0d9",
    "customerFirstName": "Frank",
    "customerLastName": "Johnson",
    "customerEmail": "frank.johnson+1@mailcrop.cc",
    "transactionReference": "557dd54d-d27f-4ae2-96f0-eeb72f0c15c1",
    "url": "https://merchant.tgipay.com/pay-now/1ebf7d26-1581-41b1-8811-4f882a85b0d9",
    "callBackUrl": "https://your-merchant-site.com/callback",
    "amount": 1000,
    "currency": "NGN",
    "website": "https://your-merchant-site.com",
    "source": null
  }
}

Step 3: Redirect Customer to Payment Page
Use the url value returned by your backend to redirect the customer to the TGIPAY payment interface. You can implement this in two ways:

Option A: Full Page Redirect

window.location.href = paymentData.data.url;

Option B: Popup Window

const paymentWindow = window.open(
  paymentData.data.url,
  'tgipay-payment',
  'width=600,height=700,scrollbars=yes,resizable=yes'
);

Callback Handling
After payment completion, TGIPAY will redirect the customer back to your configured callback URL with the payment status. The tgipay query parameter is a numeric indicator and is always 1.

// Example callback URL: https://your-site.com/payment-callback?status=success&ref=123&tgipay=1
app.get('/payment-callback', (req, res) => {
  const { status, ref, tgipay } = req.query;
  
  if (status === 'success') {
    // Handle successful payment
    console.log(`Payment successful for transaction: ${transactionReference}`);
    res.redirect('/payment-success');
  } else {
    // Handle failed payment
    console.log(`Payment failed for transaction: ${transactionReference}`);
    res.redirect('/payment-failed');
  }
});

Payment Flow States
The Direct Integration UI Redirect follows these payment states:

graph TD
    A[Payment Initiated] --> B[Customer Redirected]
    B --> C[Payment Form Loaded]
    C --> D{Customer Action}
    D -->|Enters Details| E[Processing Payment]
    D -->|Cancels| F[Payment Cancelled]
    E --> G{Payment Result}
    G -->|Success| H[Payment Completed]
    G -->|Failed| I[Payment Failed]
    H --> J[Redirect to Success URL]
    I --> K[Redirect to Failure URL]
    F --> L[Redirect to Cancel URL]

State Descriptions
State	Description	Next Actions
Initiated	Payment request created, awaiting customer action	Redirect customer to payment URL
Processing	Customer is entering payment details	Monitor for completion
Completed	Payment successful, funds captured	Update order status, send confirmation
Failed	Payment attempt unsuccessful	Offer retry options, log for analysis
Cancelled	Customer cancelled the payment	Return to merchant site, log abandonment
Error Handling
Common error scenarios and their handling:

interface ErrorResponse {
  status: boolean;
  message: string;
  errors?: string[];
}

// Handle API errors
if (!response.status) {
  switch (response.message) {
    case 'Invalid integration key':
      // Handle invalid API key
      break;
    case 'Transaction already exists':
      // Handle duplicate transaction reference
      break;
    default:
      // Handle other errors
      console.error('Payment error:', response.message);
  }
}